# 05 · 대표이사님 결정 대기 — 2026-09-21

> 답을 받으면 여기서 지우고 `03_개발연혁.md`로 옮기십시오.
> 이미 답하신 것을 다시 여쭙지 마십시오 (맨 아래 목록).

## ① [최대 병목] 지식베이스 승인 — 전문가 1명 × 2주

212개 지식노드 / 40개 성분 / 15개 고민이 **전부 Draft**입니다.
그래서 앱은 "이 성분이 무엇을 한다"를 **한 마디도 할 수 없습니다.**

이것은 코드로 해결되지 않습니다. 자격 있는 사람이 읽고 승인해야 합니다.
승인되는 순간 앱이 완전히 달라집니다. **지금 가장 값어치 있는 2주입니다.**

근거: `docs/PROJECT_REPORT_KO.md` 4장 ①

## ② 저장소가 공개(public)입니다 — 사업 전략이 인터넷에 있습니다

`github.com/mooryongahn6407/kbeauty-origin` 의 `visibility`가 `public`으로
**확인되었습니다** (GitHub API, 2026-09-21).

그 안의 `docs/PROJECT_REPORT_KO.md` 5장에 **사업화 방안 전문**이 들어 있습니다 —
파트너 네트워크 전략, 매장 송객 구조, 데이터를 선주문 근거로 쓰는 계획,
태국·베트남 확장 계획, 권장 우선순위 표까지.

비밀번호나 열쇠는 없습니다. 하지만 **경쟁사가 읽으면 그대로 따라 할 수 있는
내용**입니다. 세 가지 중 고르셔야 합니다:

1. **저장소를 비공개로 전환** — 가장 안전. 단, GitHub Pages 무료 배포가
   막힐 수 있습니다(Netlify는 비공개 저장소에서도 됩니다).
2. **그 문서만 저장소에서 빼고 PC로 옮김** — 코드는 공개로 두고 전략만 감춤.
3. **그대로 둠** — 지금 상태 유지.

제가 임의로 정하지 않고 기다립니다.

## ③ 안쪽 화면도 루미나 톤으로 풀까요?

지금은 **첫 화면만** 루미나 노이가 말합니다. 질문 화면·기록 화면은 아직
말투가 딱딱합니다. 같은 톤으로 통일할지 여쭀고(2026-09-21 04:20) 답을
받지 못했습니다.

## ④ 58 vs 92 strand 문제 (SR-001)

두 출처가 서로 다른 개수를 말합니다. CLAUDE.md 규칙 5에 따라 오너가 정하기
전까지 `CANONICAL_TAXONOMY_ID = null`로 고정되어 있습니다.
코드가 임의로 한쪽을 고르면 안 됩니다.

## ⑤ 고민·성분 이름의 현지어 표기 / ⑥ 음성 품질 / ⑦ 상업화 범위

`docs/PROJECT_REPORT_KO.md` 4장 ②③④ 참조.

---

## 이미 답하신 것 (재질문 금지)

- 머지하고 배포할 것 → 했습니다
- **유료 AI 크레딧 쓰지 말 것.** 무료로, 안 되면 **만화(그림)로** 할 것
- 홍보 영상은 **앱이 더 완벽해진 뒤에** 만들 것
- 언어 선택은 들어가서 고르는 것이 아니라 **위에 버튼으로 나열**
- 음성 버튼은 **맨 위**에, 켜고 끄는 법을 친절히 안내
