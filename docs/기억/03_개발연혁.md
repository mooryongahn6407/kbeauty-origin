# 03 · 개발 연혁 — 2026-09-18 ~ 09-21

커밋은 `git log --oneline origin/main`으로 언제든 다시 볼 수 있습니다.

## 1일차 — 2026-09-18 · 기반과 13개 화면 (PR #1, #2)

하루에 전체 뼈대가 올라갔습니다. 순서대로:

| 커밋 | 무엇 |
|---|---|
| — | 마스터 DB 추출(`npm run extract:sources`), 거버넌스 게이트, 무결성 검사 |
| `8c76287` | 루틴 스튜디오 |
| `5719247` | 제품 제안 등록부 + 숫자 일관성 감사 |
| `6049670` | 자외선 보호 (노출 기록, 근거 위치 패널) |
| `f0a6c61` | 라벨 정렬 연습 |
| `d0c7c39` | AI 튜터 — 지식 검색기, 모드 계약, 헌장 §16 평가 세트 |
| `4f81d77` | 퀘스트 지도 + 숙달 원장 (점수는 만들지 않음) |
| `103e079` | 리듀서 4개를 순수 함수로, 이벤트는 drain에서 한 번만 |
| `975a223` | 가독성: 타입 스케일, 다크 테마, 읽어주기, 프랑스어 |
| `70f28c9` | 라오어 카탈로그 (미검수 초안이라고 표시) |
| `42b59ec` | 태국어 카탈로그 + 라오어 음성이 여기로 대체 |
| `7809298` | CI: typecheck · test · build · verify:sources |
| `6d330a8` | GitHub Pages 배포 |

## 2일차 — 2026-09-19 (`03f134e`)

배포 후 직접 점검하다가 발견한 모바일 문제 수정 — 터치 영역, 가로 넘침.

## 3일차 — 2026-09-20 (`f6e0046` + PR #5에 합쳐진 커밋들)

**GitHub Pages가 대표이사님 쪽에서 열리지 않았습니다.** PC에서도 폰에서도
안 보인다고 하셨고, 원인은 끝내 이 방에서 확인하지 못했습니다(이 방은 외부
주소에 접속할 수 없음 — `08_환경과한계.md` 참조). **Netlify로 옮겨서 해결.**

그리고 대표이사님의 강한 지적이 있었습니다:
*"100점 만점에 30점도 안 된다"*, *"논문 쓰러 왔나 싶다"*, *"색상이 시그머리죽죽"*.

그에 따라 하루 만에 전면 개편:

- **첫 화면을 8개 탭 → 내 피부 여정으로.** 탭이 늘어선 화면은 서류함이고,
  서류함이 "논문 같다"의 정체였습니다.
- **루미나 노이를 그렸습니다** (`src/ui/components/Lumina.tsx`). 사진이 아니라
  그림인 이유: 사진 속 얼굴은 특정한 나이와 피부이고, 폰을 든 사람이 그 둘을
  이미 갖고 있기 때문입니다. 회사 실제 아트워크가 오면 SVG만 교체하면 됩니다.
- **게임 6개를 실제로 플레이 가능하게** 만들었습니다. 마스터 DB의 퀘스트 25개는
  이미 완성된 게임 설계였고, **과학적 주장을 하나도 하지 않고** 채점할 수 있는
  형태였습니다 — 이것이 초안 데이터 위에서 게임이 성립하는 이유입니다.
  (QST-005/006/007/008/009/011/012)
- **복습 큐** (1·3·7·14·30일 간격). 틀린 성분이 다시 나옵니다.
- **수집 카드 15장 + 방문 달력 + 선물 쿠폰.**
- **홍보 영상 도구** — 실제 앱 화면 캡처 + HTML 자막 11컷을 ffmpeg로 붙여
  62초 세로 영상. 비용 0원.

## 4일차 — 2026-09-21 (PR #5 `df9b2fe`, PR #6 `92e67c0`)

대표이사님 지적 3건에 대한 대응:

**PR #5** — 언어 버튼 5개를 상단에 격자로 전부 노출(전에는 옆으로 밀려 ລາວ가
화면 밖으로 잘렸습니다), 음성 지원 버튼을 맨 위로 + 끄는 법까지 문장으로 안내,
색·글씨 전면 개편, 한글 줄바꿈 수정.
→ 이 과정에서 실제 버그 2건을 브라우저로 보다가 잡았습니다 (`06_함정과교훈.md`).

**PR #6** — 첫 화면을 **루미나 노이가 직접 인사하는 형태**로 재작성.
"라벨 탐정" 같은 전문용어 화면 이름 5개를 5개 언어 전부에서 쉬운 말로 교체.

## 5일차 — 2026-09-22 (말투 통일)

**대표이사님 결재:** 9/21 저녁 회의 정상 개최. **말투 통일(C) 승인.**
(A 지식검수자 / B 저장소 공개 / D 58-vs-92 의 결재 결과는 **확인되지 않음**)

**한 일** — 안쪽 화면(질문·고민 선택·기록)의 문구를 루미나 노이 말투로 통일.
5개 언어(en·ko·fr·lo·th) 동시 교체, 127개 항목.

말투를 세 가지로 나눈 것이 이번 작업의 핵심입니다:

| 누가 말하나 | 어디 | 예 (한국어) |
|---|---|---|
| **루미나가 묻는다** | 질문·도움말·기록 첫 문장 | "오늘 보신 것, 제가 여기 다 적어뒀어요." |
| **소비자가 답한다** | 선택지, 기록 화면 버튼 | "당겨요", "이제 레슨 보러 갈래요" |
| **안전·거버넌스는 웃지 않는다** | 진단 아님 고지, 전문가 상담 고지, 검수중 표시 | 기존 격식체 **그대로 유지** |

세 번째가 중요합니다. "이것은 진단이 아닙니다"가 "잘하셨어요"와 같은 말투면
안전 문구가 장식이 됩니다. 규칙 6(안전 > 학습 > 상업)은 순서뿐 아니라
**말투로도** 지켜집니다.

벌주지 않는 원칙(`06_함정과교훈` C4)에 따라 선택지도 손봤습니다:
자외선 차단 "하지 않습니다" → **"아직 안 해요"**. 소비자를 탓하지 않습니다.

**검증:** 테스트 578개 통과 · 타입체크 clean · 프로덕션 빌드 clean ·
Chromium으로 390×780(밝게)·320×720(어둡게)·라오어 3조합을 **기록 화면까지 클릭**,
자바스크립트 오류 0건.
