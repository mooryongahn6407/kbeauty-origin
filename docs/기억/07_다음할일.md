# 07 · 다음 할 일 — 순서대로

> **작업이 끝날 때마다 이 파일을 갱신하십시오.**

## 1. 안쪽 화면 말투를 루미나 톤으로 (대표이사님 답 대기 중 — `05_결정대기.md` ③)

지금은 첫 화면만 루미나가 말합니다. 질문 화면과 기록 화면은 아직 딱딱합니다.

- 대상: `src/ui/screens/SkinQuestScreen.tsx` (`QuestStepView`, `QuestConcernPicker`,
  `QuestRecord`), `src/localization/messages.ts`의 `skinquest.*` 키
- 참고: 첫 화면이 어떻게 됐는지는 커밋 `92e67c0`
- 5개 언어 전부 같이 바꿔야 합니다 (en · ko · fr · lo · th)

## 2. 라오어 홍보 영상 (대표이사님: "앱이 더 완벽해진 뒤")

도구는 이미 있습니다. 자막만 라오어로 바꾸면 됩니다.

```bash
# 1) scripts/promo/slides.html 의 자막을 라오어로 교체
# 2) 앱을 띄워놓고
npm run promo:video
```

- 대본·콘티: `docs/PROMO_VIDEO_SCRIPT_KO.md` (11컷, 62초, 세로 1080×1920)
- 캡처 스크립트: `scripts/promo/capture-app.mjs`
  — **주의:** 한국어 버튼 이름으로 클릭합니다. 문구를 바꾸면 여기도 고쳐야 합니다
- 비용 0원. 유료 AI·성우·촬영 없음

## 3. 지식베이스 승인 (대표이사님 결정 필요 — 이것이 최대 병목)

`05_결정대기.md` ① 참조. 코드로 못 합니다.

## 4. 남아 있는 것들 (우선순위 낮음)

- `korea-glow.com` 도메인 연결
- 구독 버튼
- GitHub Pages가 왜 대표이사님 쪽에서 안 열렸는지 — **원인 확인되지 않음**
  (Netlify로 우회했으므로 급하지 않음)
