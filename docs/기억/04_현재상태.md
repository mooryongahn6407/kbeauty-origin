# 04 · 현재 상태 — 2026-09-22 기준 (마지막 갱신: 말투 통일 작업 후)

> **작업이 끝날 때마다 이 파일을 갱신하십시오.** 날짜를 반드시 바꾸십시오.

## 살아 있는 것

| 무엇 | 값 | 확인 방법 |
|---|---|---|
| 라이브 앱 | https://cozy-treacle-80f53c.netlify.app | Netlify deploy `6ab0aefb05de4200085f9d19`, state `ready`, published `2026-09-21T04:14:15Z` |
| `origin/main` | `9e8e21e` (PR #8까지 머지됨) | `git log --oneline origin/main -1` |
| 작업 브랜치 | `claude/laughing-babbage-acxc3h` | 말투 통일 작업이 올라가 있음 |
| 테스트 | **578개 / 23개 파일 · 전부 통과** | `npx vitest run` |
| 타입·빌드 | clean | `npm run typecheck`, `npm run build` |
| Netlify 관리 | https://app.netlify.com/projects/cozy-treacle-80f53c | |
| GitHub Pages | `has_pages: true`이나 **대표이사님 쪽에서 열리지 않음** | 원인 확인되지 않음 |

**배포 방식:** `main`에 머지하면 Netlify가 자동 빌드합니다
(`netlify.toml` → `npm run build -- --base=/`). 약 30초~1분 걸립니다.
작업 브랜치에 푸시하는 것만으로는 **라이브가 바뀌지 않습니다.**

## 전 구간 작동 확인 — 2026-09-22 13:30 (말투 통일 후)

프로덕션 빌드(`npm run build -- --base=/`, 네트리파이와 같은 방식)를 띄워
Chromium으로 **세 조합을 기록 화면까지 클릭해서** 통과시켰습니다.

| 조합 | 결과 |
|---|---|
| 390×780 · 밝게 · 한국어 | ✅ 기록 화면 도달 · 쿠폰 `KG-260922-23` 발급 |
| 320×720 · 어둡게 · 한국어 | ✅ 기록 화면 도달 |
| 390×780 · 밝게 · 라오어 | ✅ 기록 화면 도달 · «미검수 번역» 고지 정상 노출 |
| 자바스크립트 오류 | ✅ 3조합 전부 0건 |

**같은 확인에서 눈으로 본 문제 2건** (둘 다 코드가 아니라 데이터 문제입니다):
- 고민 카드 이름이 영어로 나옴 — `Acne`, `Pigmentation`, `Photoaging`
- 기록 화면에 내부 지시문이 노출됨 — "자가관리 교육, 치료 주장 금지"
→ `05_결정대기.md` ④⑤

> **주의:** 네트리파이 주소를 직접 연 것은 **아닙니다 — 확인되지 않음**
> (이 방은 외부 주소 접속 불가). 대표이사님이 폰으로 열어 보시는 것이 최종 확인입니다.

## 이전 전 구간 확인 — 2026-09-21 13:45

`main` = `da677c9`과 동일한 빌드를 Chromium 390×780으로 **끝까지 클릭해서 통과**시켰습니다.
(주간보고 §7의 근거입니다. 화면이 실제로 도는지 의심되면 이 절차를 그대로 반복하십시오.)

| 단계 | 결과 |
|---|---|
| 첫 화면 인사 | ✅ "안녕하세요! 저는 코리아 글로우의 루미나 노이예요." |
| 언어 전환(한국어) | ✅ |
| 질문 진행 | ✅ "세수하고 한 시간, 아무것도 바르지 않았을 때 얼굴…" |
| 기록 화면 도달 | ✅ |
| 선물 쿠폰 발급 | ✅ 쿠폰번호 `KG-260921` 실제 발급 |
| 학습 화면 | ✅ 12개 접근 가능 |
| 성분 찾기 게임 | ✅ "이 성분표에서 보습 · Humectant 계열을 모두 찾아보세요." |
| 자바스크립트 오류 | ✅ 없음 |

> **주의:** 이것은 **같은 코드를 이 컨테이너에서 띄워 클릭한 결과**입니다.
> 네트리파이 주소를 브라우저로 직접 연 것은 **아닙니다 — 확인되지 않음**
> (이 방은 외부 주소 접속 불가). 대표이사님이 폰으로 열어 보시는 것이 최종 확인입니다.

## 화면 13개 (`src/ui/screens/`)

| 화면 | 파일 | 근거 |
|---|---|---|
| 스킨 퀘스트 (첫 화면) | `SkinQuestScreen.tsx` | 07_CONCERNS의 `Level` 필드 |
| 성분 찾기 | `IngredientHuntScreen.tsx` | QST-008 / QST-009 |
| 둘을 나란히 놓고 보기 | `CompareLabScreen.tsx` | QST-011 |
| 순서 맞추기 | `RoutineOrderScreen.tsx` | QST-006 / QST-007 · **유일한 Approved 데이터** |
| 주장일까 기록일까 | `ClaimSorterScreen.tsx` | QST-012 · SK01 |
| 내 피부 | `MySkinScreen.tsx` | |
| 성분 하나씩 보기 | `IngredientGardenScreen.tsx` | 06_INGREDIENTS |
| 내 루틴 짜보기 | `RoutineStudioScreen.tsx` | 12_ROUTINES |
| 자외선 보호 | `SunProtectionScreen.tsx` | |
| 화장품 상자 읽기 | `LabelDetectiveScreen.tsx` | QST-005 · SPECIMEN-001/002 |
| AI 튜터 | `AITutorScreen.tsx` | 헌장 §16 |
| 내가 어디까지 왔나 | `QuestMasteryScreen.tsx` | |
| 콘텐츠 거버넌스 (관리자) | `GovernanceScreen.tsx` | |

## 지금 작동하는 소비자 기능

- **언어 5개**가 맨 위 격자에 전부 보임 (320px 폰 + 최대 글씨에서도 확인)
- **음성 지원 마스터 스위치**가 언어 바로 밑. 켜면 화면이 바뀔 때마다 자동으로
  읽어줌. 끄는 법을 문장으로 안내. 라오어는 태국어 음성으로 읽는다고 **미리** 고지
- **루미나 노이**가 첫 화면에서 직접 인사
- **복습 큐** — 틀린 성분이 1·3·7·14·30일 뒤 다시 나옴 (`src/app/review-queue.ts`)
- **수집 카드 15장** + 방문 달력 + 완주 선물 쿠폰 (`src/app/collection.ts`)
- 밝은/어두운 모드, 글씨 크기 3단계

## 데이터의 진실 (가장 중요)

| 항목 | 개수 | 상태 |
|---|---|---|
| 지식 노드 | 212 | **전부 Draft.** 제목만 있고 본문은 템플릿 문구 |
| 콘텐츠 원자 | 20 | **전부 Draft.** `Core_Explanation`이 20개 다 같은 자리표시자 |
| 성분 | 40 | **전부 Draft.** `Reference_URL`이 있는 것은 3개 |
| 고민 | 15 | **전부 Draft.** Basic/Intermediate/Advanced 등급만 있음 |
| **루틴** | **10** | **유일하게 `Status=Approved`** |
| 퀘스트 | 25 | 실제 게임 설계가 들어 있음 |

→ **앱이 추상적으로 읽히는 진짜 이유가 이것입니다.** 코드 문제가 아닙니다.
